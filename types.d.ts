type HeroData = {
  slogan: string;
  image: SanityImageSource;
};

type RecipePreview = {
  title: string;
  image: SanityImageSource;
  _updatedAt: string;
  current_slug: string;
  intro_description: string;
  rating: number;
  prep_time: number;
};
